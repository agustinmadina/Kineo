<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/reports"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Reportes" />
        <item
            android:id="@+id/choose_other_patient"
            android:icon="@drawable/ic_menu_camera"
            android:title="Elegir otro paciente" />
        <item
            android:id="@+id/edit_patient"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Editar detalles de paciente" />
        <item
            android:id="@+id/patient_progress"
            android:icon="@drawable/ic_menu_slideshow"
            android:title="Ver Historial" />
    </group>

    <item android:title="Cuenta">
        <menu>
            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_menu_send"
                android:title="Logout" />
        </menu>
    </item>

</menu>
